%\usepackage{array}                  % >{blah}, <{blah} in array formats.
%\usepackage{amsmath} % not sure about this.
\usepackage{amssymb} % \square, \blacksquare
\usepackage{accents}                %\underaccent
\usepackage{anyfontsize}
\usepackage{booktabs}               % \midrule
\usepackage{latexsym}               % \rightsquigarrow
\usepackage{mathpartir}             % \begin{mathpar}, \infer, etc.
\usepackage{mathtools}              % \dblcolon, \Coloneqq
\usepackage{nccmath}                % fix align* (etc.) spacing
\usepackage{stmaryrd}               % \shortrightarrow, \llbracket, etc.
\usepackage{adjustbox}

% Use the normal font for bold stmry symbols to avoid warnings about the bold
% font not existing.
% https://tex.stackexchange.com/questions/106714/stmaryrd-and-boldsymbol-avoid-warnings
\SetSymbolFont{stmry}{bold}{U}{stmry}{m}{n}


% Default to vertically centered colons. Doesn't quite work with euler, but
% better than nothing.
\mathtoolsset{centercolon}

% Make section name references use \S.
\crefname{section}{\S\@gobble}{\S\@gobble}

% hang is only relevant for long captions, which I don't have
\usepackage[labelsep=period,font={it},format=hang]{caption}
%\usepackage[labelsep=period,font=rm,labelfont=sc,format=hang]{caption}

% Unfortunately, only pdftex supports most of the features of microtype.
\usepackage{iftex}
\ifPDFTeX\PassOptionsToPackage{spacing=true,tracking=true,letterspace=25,}{microtype}\else\fi
\usepackage{microtype}
\frenchspacing

%% Controls hyphenation and date formatting defaults, among other things.
\usepackage[british]{babel}
% A minimum of 3 letters before & 3 after a hyphenated line-break.
\renewcommand\britishhyphenmins{33}
\hyphenation{data-fun semi-naive power-set vary-ing boo-le-ans co-mo-nad
bi-carte-sian se-man-tics eq-uip-ped}
%\hyphenation{semi-na\"ive} % "Improper hyphenation"?

% Make date formatted like "10^{th} June 2019".
% TODO: can babel do this?
\usepackage[en-GB]{datetime2}
\DTMlangsetup[en-GB]{ord=omit} % ord={level,raise,omit,sc}


%% ===== Fiddling with lists =====
\usepackage{enumitem}
\SetEnumitemKey{flushleft}{labelindent=0pt,leftmargin=*,}
\setlist[description]{font=\normalfont\itshape,}

\setlist{labelsep=.8em,}
%\setlist{labelsep=.667em,}

%% Default to flush-left?
%\setlist[enumerate]{flushleft,}
%\setlist[itemize]{leftmargin=\parindent,labelsep=.6em,labelindent=*}
%\setlist[itemize]{labelsep=*,leftmargin=\parindent,labelindent=0pt,}

%% %% Label offset matches paragraph indent.
%% \setlist[itemize,enumerate]{labelindent=\parindent, leftmargin=*,}


\usepackage{notation}
