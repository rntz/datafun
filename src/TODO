- support declarations properly

  + change 'compile-expr & co to take an env rather than a list, so we can
    support "free variables"

  + change parse-expr & co to take env rather than list, so we can parse w/ free
    vars, too.
